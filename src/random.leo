circuit RandomQuickSort {
    array: field;
    start: field[0];
    end: field[-1];

    if self = 1 {
        return self;
    } else if {
        i = random number in range(1, n)
        X = self[i]
        return 
    }
    
}